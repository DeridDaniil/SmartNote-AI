# Required — Google Gemini API key used by api/generate.ts and api/generate-stream.ts.
# Server-only: never rename to a VITE_* variant (that would ship it to the browser).
GEMINI_API_KEY=your_gemini_api_key_here
